<form method="post" action="{{ path('app_sequence_label_delete', {'id': sequence_label.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sequence_label.id) }}">
    <button class="btn">Delete</button>
</form>
